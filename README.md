# nyc-incident-hotspot-classifier
Investigated whether small, interpretable ML models can match black-box accuracy on real NYC crime data. Built and evaluated 19 models across 3 datasets (complaints, 311, shootings), caught and fixed a data leakage bug, and used PR-AUC + spatial validation. Finding: the right model depends on the decision's stakes, not just accuracy.
